---
interface Props{
    name: string;
}

const { name } = Astro.props;

---

<span class="text-text-muted px-4 py-1 bg-surface-elevated rounded-md w-fit backdrop-filter backdrop-blur-md">{name}</span>